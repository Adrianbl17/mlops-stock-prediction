name: "mlops-stock-prediction"

services:
  api:
    build: ./api
    image: joppeiemants64/mlops-stock-api:latest
    expose:
      - "8000"
    env_file: .env
    depends_on:
      postgres:
        condition: service_healthy
    networks:
      - web
      - data

  nginx:
    build: ./nginx
    image: adrianb1719/mlops-stock-nginx:latest
    ports:
      - "80:80"
    depends_on:
      - api
    networks:
      - web

  postgres:
    image: 'postgres:17.0-alpine'
    env_file: .env
    volumes:
      - postgres:/var/lib/postgresql/data/
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U stock_user -d predictions"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - data

volumes:
  postgres:

networks:
  web:
  data:
